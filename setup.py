
from setuptools import setup

setup(
    name='MathTools',
    version='1.0',
    packages=['MathTools'],
    install_requires=[
        'numpy',
        'scipy',
        # Add other dependencies here if your package relies on them
    ],
)
